% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_cox_forest.R
\name{plot_cox_forest}
\alias{plot_cox_forest}
\title{Forest Plot for Cox Model (Hazard Ratios with 95\% CI)}
\usage{
plot_cox_forest(
  fit,
  title = NULL,
  xlab = "Hazard Ratio (HR)",
  show_pvalue = TRUE
)
}
\arguments{
\item{fit}{A Cox model object (class \code{coxph}).}

\item{title}{Optional title for the plot.}

\item{xlab}{Label for the x-axis (default = "Hazard Ratio (HR)").}

\item{show_pvalue}{Logical; if TRUE, adds p-values as labels.}
}
\value{
A \code{ggplot} object.
}
\description{
Creates a clean forest plot of hazard ratios from a Cox model,
including 95\% confidence intervals and significance markers.
}
\examples{
\dontshow{if (requireNamespace("survival", quietly = TRUE) && requireNamespace("ggplot2", quietly = TRUE)) withAutoprint(\{ # examplesIf}
data(heart, package = "survival")
fit <- survival::coxph(survival::Surv(start, stop, event) ~ age + year + surgery, data = heart)
p <- plot_cox_forest(fit, title = "Cox Time-Dependent Model")
print(p)
\dontshow{\}) # examplesIf}
}
